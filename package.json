{
  "name": "dssm-intelligence-system",
  "version": "1.0.0",
  "description": "DSSM Intelligence System — Q&A reasoning engine for the Deep Symbolic Systems Model",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
